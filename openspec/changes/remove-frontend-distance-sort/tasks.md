## 1. Remove frontend distance sort

- [x] 1.1 Delete the `calculateDistanceKm()` function from `App.tsx` (lines 35–52)
- [x] 1.2 Delete the "near me" re-sort block from `App.tsx` (the `if (locationToUse && isNearMeSearch)` block that remaps and re-sorts data)
